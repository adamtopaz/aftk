# Demo framework fixture

Update the small Lean demo project in a controlled way.

Goals:
- change the greeting string in `Demo/Basic.lean`
- validate the project with `lake build`
- keep the change local and auditable
